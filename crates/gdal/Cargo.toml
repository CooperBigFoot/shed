[package]
name = "shed-gdal"
version = "0.1.0"
edition = "2024"

[dependencies]
shed-core = { path = "../core" }
hfx-core.workspace = true
gdal = { version = "0.19", features = ["bindgen"] }
geo = "0.29"
thiserror = "2"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
